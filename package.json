{
  "name": "jodl-electron",
  "version": "1.0.0",
  "description": "YouTube Downloader by Johanes Steven",
  "main": "src/main.js",
  "scripts": {
    "start": "electron .",
    "build": "electron-builder --config build-bundled.json",
    "build:lite": "electron-builder --config build-lite.json"
  },
  "devDependencies": {
    "electron": "^28.0.0",
    "electron-builder": "^24.0.0",
    "dmg-license": "^1.0.11"
  }
}
